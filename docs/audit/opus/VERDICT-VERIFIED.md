# 🔎 راستیآزمایی ممیزی opus-4.6-thinking — ZAYCHE (۲۰۲۶-۰۸-۱۷)

> هر ادعای AI خارجی باید claim-by-claim علیه کد/دیتا چک شود (قانون پروژه).
> روش: مراجعه مستقیم به کد، تست زنده، dumps دیتابیس. خروجی: TRUE / FALSE / PARTIAL با دلیل.

## خلاصه
opus-4.6-thinking (۶ فراخوان، ~$3.0) ۴۱ یافته داد و حکم «NOT READY — ۹ بلاکر» صادر کرد.
**بررسی عمیق نشان داد: از ۹ «بلاکر»، ۴ مورد FALSE یا اغراقشده** (سیستم زنده خلافش را ثابت میکند)،
**۳ مورد REAL** (یک باگ پولی، یک باگ خاموش استارتاپ، یک باگ UI)،
و بقیه جزو سختسازی (P2/P3). حکم منصفانه: **CONDITIONAL با لیست کوتاه**، نه NOT READY.

## جدول راستیآزمایی (پرمخاطرهترینها)

| # | ادعای opus | حکم | شاهد |
|---|---|---|---|
| F-05 | OmniProvider در هر تماس crash میکند (پیشفرض gemini = صفر گزارش) | **FALSE** | E2E زنده: گزارش done با ۱۵/۱۵ سکشن omni، چت 200 در 4.2s، RAG bench روی ۶۰۵ چانک — همه با OmniProvider. `super().complete()` صرفاً کوروتین بیاثر است (dead code، نه crash) |
| F-01/03 | readiness/ARQ pool با asyncio-run crash میکند | **FALSE** | readiness یک endpoint همزمان است (threadpool uvicorn → asyncio.run در آنجا مجاز). کارگر ARQ واقعاً گزارشها را کامل میکند (PDF در R2) |
| F-07 | OTP_DEV_MODE در prod فعال = auth bypass | **FALSE** | `.env`: `OTP_DEV_MODE = false` |
| F-06 | asyncio.run در chat/generator میترکد | **PARTIAL (P2)** | الگو ریسکی است ولی در مسیر فعلی (threadpool) کار میکند؛ سختسازی برای آینده |
| F-29 | pay_with_balance به credit-pack و yearly اعتبار/اشتراک نمیدهد | **TRUE — P1 (پولی)** | `order.plan_key == "monthly"` فقط؛ سالانه و `credit3`/`credit6` (۸ سفارش واقعی در DB) پرداخت میشوند ولی چیزی روی اکانت نمیآید |
| F-11 | seed_plans از session بسته استفاده میکند | **TRUE — P1 (خاموش)** | خط ۴۹: `s.exec` بعد از بستهشدن `with` → exception بگیرشده → کوپن LANCH20 هرگز سید نمیشود (ضرر نیست، ولی قابلیت مرده) |
| F-37 | دکمهٔ تأیید لغو اشتراک هرگز ظاهر نمیشود | **TRUE — P1 (UI)** | `$el.dataset.confirm` روی خود دکمه چک میشود ولی cancel() آن را روی parent ست میکند |
| F-26 | اعتبارسنجی خانه با whole-sign در برابر Placidus واقعی | **TRUE — P1 (کیفیت/هزینه)** | `house_of()` صریحاً whole-sign است (کامنت خود کد) در حالی که چارت Placidus است → فلگهای اشتباه توهم → ریترایهای بیهوده LLM |
| F-20 | race روی metrics در asyncio.gather | **TRUE — P1 (کم)** | `metrics["calls"] += 1` غیراتمی بین taskها؛ از دست رفتن آمار هزینه |
| F-35 | access_token در URL اشتراکگذاری | **PARTIAL (P2)** | واقعیت: توکن به لینک share میرود؛ اما این توکن read-only برای نماست (همان مکانیزم لینک اشتراک) — باید سند شود که scope آن را گسترش ندهیم |
| F-28 | فرمت تلفن بررسی نمیشود | **PARTIAL (P3)** | rate-limit سراسری هست؛ Kavenegar فقط شمارهٔ ایرانی میپذیرد؛ قالببندی یک سختسازی خوب است |
| F-31 | سکرت وبهوک بررسی نمیشود | **FALSE** | تلگرام با secret_token ست شده (X-Telegram-Bot-Api-Secret-Token چک میشود)؛ **Bale اصلاً هدر سکرت پشتیبانی نمیکند** (مستند) — قابل رفع نیست |
| F-16 | tz_name هرگز ست نمیشود | **FALSE** | tz_name از chart_json birth خوانده میشود (پیشفرض تهران برای شهرهای خارجی = P2) |
| F-08 | `{{ w.text\|safe }}` روی متن هفتگی LLM | **TRUE — P2** | ریسک واقعی خروجی مخرب مدل؛ باید escape شود (فقط <br> مجاز) |
| F-09 | innerHTML در ادمین | **PARTIAL (P2)** | الگو هست ولی دادهها عمدتاً خروجی API خودمان هستند؛ پاکسازی توصیه میشود |
| F-25 | عدم تطابق case در اعتبارسنجی نشانهها | **PARTIAL (P2)** | طبیعیسازی تناقض دارد؛ باگ واقعی ولی تأثیر کم |

## حکم نهایی منصفانه
- معماری، موتور نجومی، SEO، تستها، امنیت پایه (IDOR/CSRF/cookie): **قوی** ✅
- **۳ باگ واقعی که قبل از launch باید بسته شوند:** F-29 (پولی)، F-11 (کوپن)، F-37 (UI) + ارتقای F-26 (کیفیت/هزینه)
- بقیه: سختسازی در هفتهٔ اول پس از launch (نه بلاکر)
- **CONDITIONAL-GO:** با بستن F-29/F-11/F-37 و مستندسازی F-35/F-08، سیستم برای Launch آماده است.