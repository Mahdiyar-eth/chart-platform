# ACTION-PLAN — بر اساس تحلیل DeepSeek V4 Pro (راستی‌آزمایی‌شده در کد)

> هر ادعای گزارش با کد بررسی شد: ✅ تأیید / ❌ غلط / ⚠️ ناقص. فقط موارد تأییدشده وارد پلن شدند.

## P0 — بحرانی (اول)

| # | آیتم | وضعیت | فیکس |
|---|------|--------|------|
| 1 | **کالبک پرداخت**: تکرار verify → تمدید دوباره‌ی اشتراک ماهانه (+۳۰ روز دوبار) | ✅ تأیید | گارد `if not order.monthly_extended` (فیلد جدید) — فقط یک‌بار فعال‌سازی |
| 2 | **enqueue قبل از commit** + نادیده گرفتن نتیجه → گزارش paid بلاگرفت اگر Redis پایین باشد | ✅ تأیید | `commit` اول، بعد enqueue با بررسی نتیجه + job آشتی (دستور ادمین «بازتولید») |
| 3 | **کنترل دسترسی شیءمحور**: لینک اشتراک چارت → دانلود PDF گزارش پولی بدون مالکیت | ✅ تأیید | گیت `user_id` مالک + بررسی در PDF/DOCX/audio + `X-Robots-Tag: noindex` |
| 4 | **چت AI با هر پلن paid** (طبق پلن فقط gold) | ✅ تأیید | گیت `plan_key == "gold"` (یا monthly) در api_chat / api_chat_access |
| 5 | **وب‌هوک بله بدون احراز** (مستند، محدودیت API بله) + تلگرام مشروط | ✅ تأیید (مستند) | IP allowlist سرور تلگرام/بله + try/except + dedupe با update_id |

## P1 — مهم

| # | آیتم | وضعیت | فیکس |
|---|------|--------|------|
| 6 | **باگ ویجت donut عناصر**: هر سیاره مقدار ۱ با کلید برج (نه عنصر) — جمع‌های تکراری حذف می‌شوند | ✅ تأیید | شمارش واقعی عناصر (آتش/خاک/هوا/آب) از پلنِت‌ها |
| 7 | **CSRF روی POSTهای مبتنی کوکی** (سفارش/چت/synastry) | ✅ تأیید | توکن CSRF + SameSite=Strict |
| 8 | **Rate Limit** روی rectify/chat/synastry/share/audio | ✅ تأیید | محدودیت ساده per-IP در حافظه/ردیس |
| 9 | **Prompt injection**: نام/حوزه‌ها بدون محدودیت طول وارد پرامپت | ✅ تأیید | cap طول + جداکردن ورودی در پرامپت |
| 10 | **KPI**: داشبورد هست ولی قیف تبدیل/سلامت worker/تقسیم پلن ندارد | ⚠️ ناقص (کد دارد) | افزودن کارت‌های قیف + سلامت صف + هزینه per-plan |
| 11 | **صوت در /tmp + race** | ⚠️ ناقص | فایل با UUID + پاک‌سازی دوره‌ای + انتقال به R2 (بهتر) |
| 12 | **ایندکس‌های مرکب** روی Order(chart_id,status) و Report | ⚠️ ناقص | ALTER TABLE + ایندکس |

## P2 — بهبود

| # | آیتم | وضعیت |
|---|------|--------|
| 13 | fallback دامنه در sitemap فقط dev (env ست است) | ❌ غلط (فقط دفاعی) — fix دفاعی کوچک |
| 14 | «monthly/synastry seed نشده» | ❌ غلط — هست |
| 15 | «preview نمایش داده نمی‌شود» | ❌ غلط — هست |
| 16 | slug سِنبله `sowza` | ❌ غلط — استاندارد خود سایت |
| 17 | SSRF در رندر PDF/DOCX | ⚠️ نیاز بررسی عمیق renderer |
| 18 | کش سرویس‌ورکر | ⚠️ بررسی + استثنا برای /api و /chart |

## محتوایی (approved قبلی)
- ✅ صفحات راهنما/درباره/FAQ + دکمه‌های راهنما — **ساخته و لایو شد**
- ⏳ محتوای مارکتینگی V4 Pro + مقالات SEO + تصاویر — در حال اجرا
- ⏳ گزارش صفر-تا-صد ساده (PLAIN-REPORT.md — پیش‌نویس آماده)
- ⏳ تست نهایی دسکتاپ+موبایل

## ترتیب اجرا
۱) P0-1..5 (امنیت/پول) → ۲) P1-6..12 → ۳) P2 → ۴) مقالات/تصاویر → ۵) تست نهایی → ۶) گزارش نهایی
