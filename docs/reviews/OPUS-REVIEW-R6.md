# 🔍 OPUS-REVIEW-R6 — بازبینی دور ۵

**بازبین:** Opus 5 · **تاریخ:** ۲۰۲۶-۰۸-۲۳ · **حالت:** REVIEW-ONLY (هیچ کدی تغییر نکرد)
**دامنه:** `8afff57..30ec8da` (PR #1، merge شده) · ۲۶ فایل · ‎+۹۱۵ / ‎−۵۳
**پاسخ به:** اجرای `V1`–`V10` از `OPUS-REVIEW-R5`

---

## STATUS

| محور | نتیجه |
|---|---|
| `V1`–`V10` | **۱۰ از ۱۰ — همه با اجرای مستقل تأیید شد** |
| `AC-1` تا `AC-5` | **۴ کاملاً برقرار · ۱ برقرار ولی معیارش شکننده است** (`AC-4`) |
| سوئیت تست (اجرای من) | 🟢 **۷۱۴ passed · ۳ skipped · ۰ failed** — **دقیقاً برابر ادعا** |
| CI از مخزن | 🟢 run #254 روی `30ec8da` → **SUCCESS** (تأیید از GitHub API) |
| گیت drift (P0 دور قبل) | 🟢 **واقعاً فیکس شد** — تست منفی من: `exit=1` |
| **یافتهٔ جدید P0/P1** | 🟢 **هیچ** |

**حکم:** 🟢 **دور ۵ کاملاً پذیرفته می‌شود.**
این اولین دوری است که **هیچ یافتهٔ بحرانی یا مهمی پیدا نکردم**. هر ادعایی که هرمس کرد،
مستقل بازتولید شد و درست بود — از جمله عددی که خودش گزارش کرده بود (۷۱۴+۳) که برای
اولین بار **دقیقاً** با اندازه‌گیری من یکی شد.

---

## TRUTH-NOTES

**آنچه اجرا کردم:**
1. ✅ PostgreSQL 16 + pgvector + Redis + venv؛ **سوئیت کامل: `714 passed, 3 skipped` در ۵۷.۶۴ ثانیه**
2. ✅ **تست منفی گیت drift** (همان تستی که دور قبل باگ را پیدا کرد) — مدل بدون مهاجرت اضافه، اجرا، revert
3. ✅ **راستی‌آزمایی CI از GitHub API**: run **#254** روی `30ec8da` → `conclusion: success`
4. ✅ اجرای ۱۲ تست جدید دور ۵ (drift-gate، media، brand، ترتیب وزن) — همه سبز
5. ✅ بوت اپ + Playwright/Chromium روی صفحهٔ گذرها در ۳۹۰px + اندازه‌گیری مختصات واقعی
6. ✅ بررسی نحوی همهٔ `scripts/*.sh`

**آنچه اجرا نکردم:**
1. ❌ `bash scripts/ci.sh` کامل (اجزای کلیدی‌اش را جدا اجرا کردم)
2. ❌ هیچ فراخوانی LLM پولی
3. ❌ **استقرار prod را راستی‌آزمایی نکردم** — به سرور تولید دسترسی ندارم. ادعای
   «prod لایو با کد جدید، `/media` صحیح ۴۰۴، صفحهٔ گذرها بدون ۵۰۰» **تأیید نشده**؛
   فقط می‌توانم بگویم کدی که deploy شده، همان کدی است که من سبز دیدم.
4. ❌ صحت نجومی خروجی گذر.

---

## FINDINGS

### 🟢 V1/V2/V3 — گیت drift واقعاً فیکس شد (تأیید با تست منفی)

همان تستی را زدم که دور قبل باگ را لو داد:

```
=== POSITIVE: کد سالم ===
EXIT=0
==> drift gate OK

=== NEGATIVE: مدل بدون مهاجرت ===
EXIT=1                                          ← ✅ (دور قبل: 0)
FAILED: New upgrade operations detected: [('add_table', '_opus_drift_probe' ...
DRIFT-GATE: FAILED (new upgrade operations detected)
--- شمارش "drift gate OK": 0                    ← ✅ دیگر چاپ نمی‌شود
```

**V2 هم کامل است.** ابتدا فکر کردم `failure-drill.sh:29,71` هنوز `&& echo` بدون محافظ دارند،
اما با خواندن خطوط ادامه (`\`) معلوم شد هر چهار مورد `|| { echo FAILED; exit 1; }` دارند:
```
scripts/failure-drill.sh:29-30   && echo "  alembic_version restored OK" \
                                 || { echo "  FAILED: ..."; exit 1; }
scripts/failure-drill.sh:71-72   && echo "  alembic version untouched ✅" \
                                 || { echo "  FAILED: ..."; exit 1; }
```
⇒ **هیچ گیت بی‌صدایی باقی نمانده.** ✅

**V3:** `tests/test_drift_gate_negative.py` وجود دارد و در اجرای من سبز است — یعنی
از این پس اگر گیت دوباره بی‌صدا شود، **خودِ سوئیت** می‌گیردش. این دقیقاً همان
«قانون گیت را با شکست تست کن» است که دور قبل نوشتم. ✅

---

### 🟢 V4–V7 — همه تأیید شد

| مورد | تأیید |
|---|---|
| **V4** رسانهٔ محلی خواندنی | روت `GET /media/{key:path}` در `app/routes/cms_admin.py:315` اضافه شد؛ `tests/test_media_fallback_r5.py` رفت‌وبرگشت (نوشتن → همان بایت‌ها) را اثبات می‌کند و در اجرای من سبز است. حالت «می‌نویسد ولی خوانده نمی‌شود» رفع شد ✅ |
| **V5** VAPID | پذیرش کورکورانهٔ ۵۰۳ برداشته شد؛ تست اختصاصی با `skipif` اضافه شد ⇒ خرابی تولید دیگر پنهان نمی‌ماند ✅ |
| **V6** share | ۵۰۳ به‌جای ۴۰۴ برای «رندرر در دسترس نیست» — معنای درست ✅ |
| **V7** زبان برند | `scripts/brand_language_gate.py` + `scripts/brand_allowlist.txt` (۸ خط، `فایل:خط`) جای whitelist مبتنی بر substring را گرفت؛ `tests/test_brand_gate_r5.py` سبز ✅ |

---

### 🟢 V8/V9/V10 — صفحهٔ گذرها حالا واقعاً می‌فروشد (اندازه‌گیری مرورگری)

| ادعا | اندازه‌گیری من (۳۹۰px، Chromium) |
|---|---|
| نمونهٔ تحلیل زیر CTA | ✅ **`y=364`** (ادعا: ۳۵۱ · **دور قبل: ۴۴۰۵**) |
| CTA بالای صفحه | ✅ `y=254` |
| بخش «۵ گذر مهمِ امسال» با وزن سراسری | ✅ موجود در `y=634` |
| ماه‌های جمع‌شونده | ✅ **۱۱ `<details>` · ۳ باز به‌صورت پیش‌فرض** (ماه جاری + ۲ ماه بعد) |
| خطای کنسول | ✅ ۰ خطای واقعی (۲ مورد محیطی: ۴۰۱ روی `credits/me` برای مهمان + بلاک‌شدن دامنهٔ آنالیتیکس توسط پراکسی) |
| اسکرول افقی | ✅ ندارد |

**بصری (`docs/reviews/evidence/r6-transits-verified-390.png`):** ساختار حالا دقیقاً
منطق فروش دارد — انتخابگر ماه → CTA طلایی → **جعبهٔ نمونهٔ تحلیل با روایت واقعی
(«زحل در نسبت با خورشید — مرور بنیان‌ها») و سؤال تأمل و دکمهٔ باز کردن** →
«۵ گذر مهم امسال» → تایم‌لاین رایگان با ماه‌های جمع‌شونده.
تفاوتش با دیوار ۳۲ کارتی دور قبل، زمین تا آسمان است.

---

### 🟡 تنها یافته‌های این دور (هر دو کوچک)

| # | یافته | جزئیات |
|---|---|---|
| **N1** | **معیار `AC-4` شکننده است** (نه نقض‌شده). ادعا: ارتفاع `2994 < 3000`. اندازه‌گیری من روی چارت خودم: **`3262px`**. علت: ارتفاع به **تعداد رویدادهای ۳ ماه بازشده** بستگی دارد و آن هم به دادهٔ چارت. ⇒ هدف عملاً محقق شده (از ۵۴۳۹ به ~۳۲۰۰)، ولی معیار «< ۳۰۰۰ پیکسل» **وابسته به داده** است و روی چارت دیگری می‌شکند. معیار باید **ساختاری** باشد نه پیکسلی: «حداکثر ۳ ماه باز، بقیه `<details>`» — که همین الان برقرار است ✅ |
| **N2** | در فهرست ماه‌ها، **«آبان» دو بار** دیده می‌شود (ابتدای بازه و انتهای بازهٔ ۱۲ماهه = آبان ۱۴۰۵ و آبان ۱۴۰۶) بدون برچسب سال. برای کاربر گیج‌کننده است. پیشنهاد: سال را کنار نام ماه بیاور وقتی بازه دو سال را در بر می‌گیرد |

---

### ✅ تأیید صداقت گزارش

- **`scripts/runtime_withdrawal_race.sh` خطای نحوی دارد و واقعاً از قبل موجود بوده.**
  راستی‌آزمایی کردم: `git show 8afff57:...` هم همان خطا را در خط ۹ می‌دهد، و آخرین کامیتی
  که فایل را لمس کرده `16c8371` است (کاملاً بی‌ربط به این دورها). **افشای هرمس دقیق بود.** ✅
  (این فایل در هیچ گیت CI صدا زده نمی‌شود، پس ریسک فعلی ندارد — ولی یک اسکریپت drill
  که parse نمی‌شود یعنی آن drill هرگز قابل اجرا نبوده.)
- عدد `714 passed, 3 skipped` **دقیقاً** با اندازه‌گیری مستقل من یکی شد — اولین بار در ۶ دور.

---

## REQUIRED_FIXES

**هیچ فیکس اجباری‌ای وجود ندارد.** موارد زیر اختیاری و کم‌اهمیت‌اند:

| ID | فیکس | یافته |
|---|---|---|
| `U1` | `AC-4` را از «ارتفاع < ۳۰۰۰px» به معیار ساختاری تغییر بده: «حداکثر ۳ گروه ماه باز است و بقیه `<details>` بسته‌اند» + تست واحد روی همین | N1 |
| `U2` | برچسب سال کنار نام ماه وقتی بازه دو سال را می‌پوشاند | N2 |
| `U3` | `scripts/runtime_withdrawal_race.sh` خط ۹ را درست کن (نقل‌قول تودرتو در `python3 -c`) — یا اگر drill منسوخ است، حذفش کن | صداقت |
| `U4` | یک گیت `bash -n` روی همهٔ `scripts/*.sh` به `ci.sh` اضافه کن تا اسکریپت غیرقابل‌parse دوباره سال‌ها بی‌صدا نماند | U3 |

---

## ACCEPTANCE_CRITERIA (برای U1–U4)

```
AC-1 (U1): تست واحد → تعداد <details> باز ≤ ۳ ، بقیه بسته؛ مستقل از دادهٔ چارت
AC-2 (U2): چارتی که بازه‌اش دو سال را می‌پوشاند → هر دو «آبان» برچسب سال دارند
AC-3 (U3): bash -n scripts/runtime_withdrawal_race.sh → بدون خطا
AC-4 (U4): یک اسکریپت عمداً خراب اضافه کن → ci.sh قرمز شود (تست منفی، طبق قانون دور ۵)
```

---

## 📌 جمع‌بندی ۶ دور — مسیر فنی بسته شد

| دور | یافته‌های من | وضعیت |
|---|---|---|
| R1 | ۷ بحرانی + ۸ مهم + ۱۲ کوچک | بسته |
| R2 | ۲ بحرانی جدید (از فیکس‌ها) + ۹ | بسته |
| R3 | ۳ بحرانی (چرخهٔ عمر داده + مهاجرت) | بسته |
| R4 | ۴ بحرانی (CI ساختگی، صفحات ۵۰۰) | بسته |
| R5 | ۱ بحرانی (گیت drift بی‌صدا) | بسته |
| **R6** | **هیچ** | ✅ |

**کیفیت مهندسی مسیر پول، مهاجرت‌ها، گیت‌ها و CI حالا در وضعیت سالمی است.**
سوئیت ۷۱۴ تستی از خودِ مخزن سبز است، گیت drift با تست منفی محافظت می‌شود،
و صفحهٔ محصول جدید در مرورگر واقعی تأیید شد.

### آنچه از پلن اصلی هنوز باقی است (فنی نیست — محصولی است)

۶ دور صرف **بستن بدهی فنی** شد. اما از ۷ Workstream پلن اولیه (`HERMES-PLAN-v1`)،
این‌ها هنوز کامل نشده‌اند و **هیچ‌کدام باگ نیستند — کار محصول‌اند**:

| Workstream | وضعیت واقعی |
|---|---|
| **A** اقتصاد اعتبار | 🟢 کامل و تست‌شده |
| **B** گذرهای پیش‌رو | 🟢 کامل — از موتور تا صفحهٔ فروش |
| **C/D** دیزاین‌سیستم و ناوبری | 🟢 کامل (توکن، تم روشن، nav واحد) |
| **E** صفحات و تست | 🟢 کامل |
| **F** محتوا | 🟠 **۵۰ مقاله موجود است ولی ممیزی کیفی `F1` هرگز اجرا نشد** — آستانه‌های تعداد کلمه، متای یکتا، لینک داخلی، Schema، واژه‌نامه |
| **G** مارکتینگ و رشد | 🟠 زیرساخت هست (رویداد قیف، لید مگنت، رفرال، SEO) ولی **`G2` بهینه‌سازی قیف با دادهٔ واقعی** هرگز انجام نشد — چون هنوز ترافیک واقعی نیست |
| **بیرونی** | 🔴 مرچنت واقعی زرین‌پال · کلید کاوه‌نگار · FCM · Search Console — **همه منتظر شما** |

### پیشنهاد من برای قدم بعد

**دور بازبینی فنی دیگری لازم نیست.** به‌جایش سه کار به‌ترتیب:

1. **راه‌اندازی واقعی** (فقط شما می‌توانید): مرچنت زرین‌پال + کاوه‌نگار + Search Console.
   بدون این‌ها هیچ‌کدام از کارهای ۶ دور گذشته درآمد تولید نمی‌کند.
2. **ممیزی محتوا (`F1`)** — تنها بخش پلن که هرگز اجرا نشد و به هیچ کلید بیرونی نیاز ندارد.
3. **بعد از رسیدن ترافیک واقعی:** `G2` (بهینه‌سازی قیف با دادهٔ Umami) — الان بدون داده حدس است.

---

*بازبینی توسط Opus 5 — با اجرای واقعی: ۷۱۴ تست، تست منفی گیت drift، GitHub Actions API، و Chromium روی صفحهٔ گذرها. استقرار prod راستی‌آزمایی نشد (TRUTH-NOTES بند ۳).*
