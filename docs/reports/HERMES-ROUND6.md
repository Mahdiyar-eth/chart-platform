# ZAYCHE — دور ۶ (پاسخ به بازبینی Opus R6) — ۱۴۰۴/۰۶/۰۱

## نتیجه: بازبینی R6 هیچ یافتهٔ بحرانی/مهمی نداشت (🟢 دور ۵ پذیرفته شد). فیکسهای اختیاری U1–U4 + ممیزی محتوای F1 انجام و راستیآزمایی شد.

**CI واقعی از مخزن:** PR #2 → GitHub Actions **run #32675020918 → SUCCESS** (729 passed · 3 skipped · «All checks passed!»).
لینک: https://github.com/Mahdiyar-eth/chart-platform/actions/runs/32675020918

## راستیآزمایی Opus R6
بازبین مستقل تأیید کرد:
- سوئیت **714 passed · 3 skipped · 0 failed** — دقیقاً برابر ادعای دور قبل
- run GitHub Actions روی `30ec8da` → **SUCCESS** (از API)
- گیت drift (P0 دور قبل) واقعاً فیکس شد (تست منفی: exit=1)
- صفحهٔ گذرها ساختار فروش دارد (نمونه بالای صفحه، «۵ گذر مهم»، ماههای جمعشونده)
- تنها نکتهها: معیار AC-4 «ارتفاع<3000px» وابسته به داده است (N1)، و «آبان» دوبار بدون سال (N2) — هر دو غیراجباری

## فیکسهای اختیاری U1–U4
| ID | فیکس | وضعیت |
|---|---|---|
| U1 | AC-4 از «ارتفاع<3000px» به معیار ساختاری «حداکثر ۳ ماه باز، بقیه `<details>`» + تست واحد مستقل از داده | ✅ |
| U2 | برچسب سال کنار نام ماه وقتی بازه دو سال را میپوشاند (آبان ۱۴۰۵ / آبان ۱۴۰۶) | ✅ |
| U3 | خط 9 `runtime_withdrawal_race.sh` — curl حذفشده بازسازی شد؛ حالا `bash -n` پاس میدهد | ✅ |
| U4 | گیت `bash -n` روی همهٔ `scripts/*.sh` در ci.sh + تست منفی | ✅ |

## ممیزی محتوای F1 (B-3)
ابزار `scripts/content_audit_f1.py` + گیت `tests/test_content_audit_f1.py`:
| معیار | وضعیت |
|---|---|
| تعداد مقاله | ۵۰ |
| تعداد کلمه | زیر۳۰۰ = 0 · ۳۰۰-۵۰۰ = 4 · ۵۰۰+ = 46 |
| عنوان/متای یکتا | 0 خالی · 0 تکراری |
| لینکهای مرتبط موضوعی (B-3 «۲-۳ مقاله مرتبط») | **قبلاً 19 مقاله لینک نامرتبط؛ رفع شد → 0 مشکل** |
| Schema (Article + Breadcrumb + FAQPage) | ✅ |

**یافتهٔ واقعی که رفع شد:** `page_article` لینکهای «مقالات مرتبط» را بهصورت دلخواه (۶ مقالهٔ اول) میآورد، نه موضوعی. حالا هماندستهها اولویت دارند (B-3).

## اضافهشده در این دور
- `scripts/content_audit_f1.py` — ابزار ممیزی محتوا (تشخیص، بدون گیت)
- `tests/test_content_audit_f1.py` — گیت محتوا (۶ تست)
- `tests/test_bash_n_gate_r6.py` — گیت bash -n (۳ تست)
- `tests/test_transit_page_sort_r5.py` — تستهای ساختاری U1 + برچسب سال U2 (۵ تست جدید)
- `app/astrology/transit_forecast.py` — `month_label_map()` + `_fa_digits` + `FA_MONTH`
- `app/routes/seo.py` — مرتبسازی هماندسته در `others`
- `scripts/ci.sh` — گیت `bash -n`
- `scripts/runtime_withdrawal_race.sh` — تعمیر خط 9
- `docs/qa/F1-CONTENT-AUDIT.md` — خروجی ممیزی

## توجه (دقت صداقت)
- دو metrics واژهنامه/اصطلاحانگلیسی در ممیزی **heuristic** هستند و در گزارش صراحتاً غیرقابل اتکا علامت خوردهاند (نمونههای فعلی false positiveاند).
- استقرار prod از سرور واقعی راستیآزمایی شد (بستن شکاف TRUTH-NOTES بند ۳ بازبین): `chart-web/worker` active، `/health` 200، `/media` 404 درست، صفحهٔ گذرها بدون 500.
