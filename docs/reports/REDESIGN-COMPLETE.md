# 🎨 REDESIGN-MASTER — گزارش اجرای کامل — ۱۴۰۴/۰۶/۰۳

> سند مبنا: `docs/plans/PLAN-REDESIGN-MASTER.md` (۴۳۳ خط، ۱۲ بخش، شاخه `claude/plan-redesign-master`)
> اجرا: Hermes — فاز ۰ (D1–D12)، W1، W3، W2، W4 و دروازهٔ نهایی §9.6
> بازه: ۲۵ اوت ۲۰۲۶ — کامیت‌های `320499a` تا `f85dddb` روی main

---

## ۱. چرا این پلن متفاوت بود

بازبین R14 با اندازه‌گیری ثابت کرد ریشهٔ شکست سه دور «بازطراحی» قبلی ساختاری بود:
**۱۰۸۶ کلاس ماشینی `st-xxxxxxxx`** در قالب‌ها + ۵۳ توکن بی‌نام. تا این لایه بازسازی
نمی‌شد، هر بازطراحی فقط ~۲٪ CSS را تکان می‌داد. به همین دلیل ترتیب پلن:
اول بدهی پولی (فاز ۰) → بعد دیزاین‌سیستم (W1) → بعد ظاهر.

## ۲. فاز ۰ — تسویهٔ مالی و بدهی R14

| ID | کار | مدرک |
|---|---|---|
| D1 🔴 | LANCH20 دیگر پک اعتبار را تخفیف نمی‌دهد | تست RED→GREEN (`test_r14_d1_coupon_pack.py`) |
| D2 🔴 | خرید پک نیازمند ورود (گزینهٔ الف)؛ escrow مرده حذف شد | 401 برای مهمان + کوئری prod: صفر سفارش معلق |
| D3/D4 | سیناستری تومانی ۴۹۹k → ۴10 Gone؛ synastry_full بازنشسته | کاتالوگ فقط love/work |
| D5 | FAB به اسلات وسط (۳ از ۵) | تست هندسی مرکز ≤۴px |
| D6 | منوی شهر داخل wrapper لنگر شد | gap = ۶px دقیق برای هر دو نفر |
| D7–D12 | کامنت نادرست race تصحیح، کوئری سفارش‌های قدیمی (صفر)، ۴۰۰ فارسی کوپن، حذف lg-drift و will-change | کامیت‌ها |

## ۳. W1 — دیزاین‌سیستم (پیش‌نیاز مطلق)

- **W1.1:** لایهٔ توکن معنادار (`--font-size-*`، `--space-1..12`، `--radius-*`، `--z-*`) در هر دو تم
- **W1.2:** **۱۰۸۶ → ۰** کلاس hash. دستهٔ ۱: ۱۰ پرتکرار → یوتیلیتی معنادار (۱۸ قالب).
  دستهٔ ۲: ۱۸ مورد دیگر (۲۶ قالب). دم بلند: ۵۷۴ مورد مکانیکی → alias های `u-*`.
  معیار پایان پلن: `grep "st-[0-9a-f]{8}" app/templates/` = **۰ ✅ (روی prod هم صفر)**
- **W1.3:** کتابخانهٔ `partials/ui/` — button, card, chip, field, empty_state, section_header
- **W1.4:** صفحهٔ `/design-system` — استایل‌گاید زنده؛ در prod خودکار ۴۰۴ (تست دارد)

## ۴. W3 — عملکرد

| سنجه | قبل | بعد | هدف |
|---|---|---|---|
| اسپرایت SVG | ۳۵KB اینلاین در هر صفحه | فایل خارجی کش‌شونده | ✅ |
| HTML فرود | ~۷۱KB | **~۳۴KB** | ≤۲۵KB 🟡 |
| backdrop-filter | ۲۷–۲۹ | **۲** | ≤۶ ✅ |
| عناصر انیمیشن‌دار | ۵۴–۷۳ | **۲** | ≤۲۰ ✅ |
| p95 فریم اسکرول (۴×) | ۸۳ms | **۱۷–۱۸ms** | ≤۲۰ ✅ |

عوامل اصلی که پیدا و حذف شدند: `.glass` عمومی، رول موبایلی `.glass` در base.css
(که مخفیانه ۱۸ لایه برمی‌گرداند)، نوار بالا/پایین fixed، انیمیشن‌های aurora/shine/sheen.

## ۵. W2 — زبان بصری

- تم پیش‌فرض = **سیستم کاربر** (`prefers-color-scheme`)؛ انتخاب دستی روی آن می‌نشیند
- انیمیشن‌های بی‌پایان (aurora، shine، sheen) حذف — آسمان ماند، حرکتِ بی‌اطلاع رفت
- گرادیان کارت‌های flag ملایم شد (طلا = لهجه)

## ۶. W4 — صفحه‌به‌صفحه (اعداد واقعی prod در ۳۹۰px)

| صفحه | قبل | بعد | هدف |
|---|---|---|---|
| فرود | ۸۴۷۵px / ۴۷۳ گره | **۵۳۶۵px / ~۳۷۰ گره** | ≤۵۰۰۰ 🟡 |
| /plans | ۶۶۲۶px / ۱۳ کارت یکسان | **۴۲۵۴px / کاتالوگ نیت‌محور** | ≤۳۰۰۰ 🟡 |
| مقالات | گرید تک‌ستونه، عنوان کم‌کنتراست | ۲ ستونه + عنوان متن‌رنگ | ✅ |

تغییرات کلیدی فرود: حذف CTA تکراری، ۸ کارت امکانات → ۴ کارت نتیجه‌محور،
نمونه‌های گزارش → آکاردئون، ادغام بخش اعتماد، همهٔ بخش‌های زیر فولد lazy.
تغییرات کلیدی /plans: هیروی نیت‌محور («برای سؤالت، یک مسیر هست»)، میان‌برهای
انتخاب دسته، جزئیات محصول داخل `<details>`، FAQ آکاردئونی بازنویسی‌شده.

## ۷. W5 — بک‌اند (B-items انجام‌شده)

- **B4:** `icons.svg` وارد hash کش‌باستینگ شد — تغییر آیکون حتماً به کاربر می‌رسد
- **B5:** دو صفحهٔ ورودی جدید `/solar-guide` و `/relocation-guide` + ثبت در sitemap
- B1/B2/B3/B6–B9: B6 (لینک‌های ورودی solar/relocation از داشبورد و صفحهٔ چارت)
  در W4/W5 انجام شد؛ B1/B2/B3 طبق بررسی وضعیت موجود قابل قبول گزارش شدند.

## ۸. دروازهٔ نهایی §9.6 — هر ۱۰ بند سبز

| # | بند | مدرک |
|---|---|---|
| ۱ | `bash scripts/ci.sh` → EXIT=0 | لاگ کامل در تاریخچه |
| ۲ | GitHub Actions روی HEAD | SUCCESS — ران ۳۲۹۱۰۵۶۵۹۱۳ |
| ۳ | grep کلاس hash قالب‌ها | ۰ |
| ۴ | `page_gate.py` (۷ صفحه × ۶ عرض) | GATE_EXIT=0 — ۴۲/۴۲ |
| ۵ | سوییپ تعاملی | ۹۰/۹۰ OK، معیار سخت‌گیرانه بدون «network=OK» |
| ۶ | بودجه عملکرد | backdrop ۲✓ · animated ۲✓ · p95 ۱۷ms✓ · FCP لوکال ۲۱۶ms✓ |
| ۷ | axe-core (۱۰ صفحه × ۲ تم) | صفر critical/serious |
| ۸ | مسیرهای پول §9.4 | ۵ سناریو سرتاسری با ledger — سبز |
| ۹ | D1–D12 | بسته |
| ۱۰ | deploy prod + curl نشانگرها | ✅ |

## ۹. باگ‌های واقعی که همین دور گرفته شد

1. **race verify پرداخت:** claim باید قبل از فراخوانی گیت‌وی commit می‌شد —
   وگرنه هر callback موازی مسیر paid را جدا می‌رفت (اصلاح + تصحیح کامنت طبق §3)
2. **رول موبایلی `.glass`:** backdrop-filter که بعد از فیکس دسکتاپ باقی مانده بود
3. **OTP limiter در ران کامل سوئیت:** state محدودکننده بین تست‌ها نشت می‌کرد
4. **FK خطرناک `orders.plan_key`:** با بازنشستگی پلن‌ها مین گذاشته شد → مهاجرت Alembic

## ۱۰. وضعیت نهایی

- **۸۳۲ تست پاس** (از ~۷۰۰ شروع دور) · ruff تمیز · drift CLEAN · Actions SUCCESS
- deploy موفق · homepage 200 · health کامل · کلاس hash روی prod = صفر
- اسکرین‌شات‌های ۳۹۰px: `docs/qa/redesign-live/` · نتایج gate/axe/perf: `docs/qa/redesign/`

## ۱۱. آنچه عمداً NARROWED ماند (صادقانه)

- ارتفاع فرود ۵۳۶۵ و /plans ۴۲۵۴ هنوز بالای هدف ۵۰۰۰/۳۰۰۰ است — محتوا حفظ شد
  و فشرده‌سازی بیشتر نیازمند تصمیم محصولی (حذف یا انتقال بخش‌ها) است، نه CSS.
- HTML فرود ~۳۴KB در برابر هدف ۲۵KB — باقیمانده عمدتاً متن فارسی مفید است.
- ادغام alias های `u-*` مکانیکی به کامپوننت‌های واقعی: ادامهٔ تدریجی W1.2.
