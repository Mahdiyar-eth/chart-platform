# ✅ گزارش اجرای پلن ممیزی انسانی صفر تا صد — HERMES-FULL-QA
**تاریخ:** ۲۰۲۶-۰۸-۲۴ · **پلن:** `docs/plans/PLAN-FULL-QA-v1.md` · **مبنای اجرا:** بیلد روی `main` (شامل R9)

## ۱. صورت‌وضعیت پلن اصلی
`docs/qa/PLAN-STATUS-AUDIT.md` — ۴۰+ بند بررسی شد.
- **DONE:** ۳۱ · **NARROWED (رفع‌شده):** ۳ (A4, A7, C5)
- **PARTIAL/PENDING:** F2 (۴ مقاله کوتاه), G2/G4/G8 (نیاز به داده/مالک), Z1
- **بزرگ‌ترین باریک‌شدگی:** «دو سیستم پولی موازی» — صفحهٔ پلن‌ها `api/orders` می‌فروخت در حالی که بک‌اند `api/purchase` بود. **در R.10 رفع شد.**

## ۲. فیکس‌ها (Q1–Q7)
Q1–Q7 در R9 بسته شدند (`test_gold_bundle_r9.py`, `test_admin_credit_a7.py`, `test_coupon_reservation.py`, `test_article_images_r9.py`, `form.html`, `test_purchase_a4.py`, `requirements.txt`).

## ۳. سیاههٔ تعاملی (P2-1)
- **۱۳۱۱ آیتم** · ۲۸ صفحه · `docs/qa/INTERACTION-INVENTORY.md` + `.json`
- ابزار: `scripts/interaction_inventory.py`

## ۴. سوییپ واقعی (P2-2)
- **۵۴ کلیک واقعی** · ۹ صفحه · `docs/qa/INTERACTION-SWEEP.md`
- **۰ BROKEN.** ۳۰ «DEAD» همگی رفتارِ درستِ حالت مهمان بودند (فرم خالی/کوپن بدون کد/بدون لاگین).

## ۵. یافته‌های واقعی پیدا و رفع‌شده
| # | منبع | مشکل | فیکس |
|---|---|---|---|
| P2-5 | `/birth-form` | تاریخ نامعتبر (ماه ۱۳/روز ۴۰/سال ۹۹۹۹) بدون خطای فارسی به بک‌اند می‌رفت | اعتبارسنجی جلالی/میلادی + روزهای ماه + سال کبیسه با پیام فارسی |
| P1-2 | `/plans` | فروش موازی تومانی `api/orders`؛ اعتباری هرگز | جدول واحد اعتباری از `credit_prices` + دکمهٔ `/api/purchase` |
| **P3-2** | `entitlements.py` | **`report_audio` (نسخهٔ صوتی) به‌جای `audio`، استحقاق `report` می‌داد** — خریدار نسخهٔ صوتی، دسترسی گزارش می‌گرفت | `_kind_for_action` برای `report_audio` → `audio` (گیت کاتالوگ↔تحویل این را گرفت) |

## ۶. پنل ادمین — ۱۳ چرخه (P2-3)
✅ **۹ تست چرخه** در `tests/test_admin_cycles_r10.py`: KPI · credit-report · credit-price change · manual grant · flags · health · prompts round-trip · content-page reachable · 403 بدون کوکی.
چرخه‌های نیازمند کلید بیرونی (TTS/LLM test) و خریدی که نیاز به دادهٔ واقعی دارد صادقانه `BLOCKED_EXTERNAL` علامت خوردند و در `ALLOWED_MISSING` گیت P3-3 هستند.

## ۷. ۹ سفر کاربر (P2-4)
✅ **۵ سفر اعتباری** در `tests/test_user_journeys_r10.py`: خرید پک اعتباری · GOLD → چت+گذر باز شد (Q1) · خرید گزارش · ناکافی → بدون بدهی و بدون استحقاق · گذر با استحقاق (بدون پرداخت مجدد).
> سفرهای نیازمند OTP واقعی/SMS/مرچنت زرین‌پال صادقانه `BLOCKED_EXTERNAL` — فقط با کلیدهای مالک قابل تست است (به چک‌لیست گوشی P2-6 مراجعه).

## ۸. NARROWED
- **A4 (P1-2):** کامل اجرا شد (نه باریک).
- **پنل ادمین (P2-3) و سفرها (P2-4):** به‌صورت تستِ انتگراسیون با کوکی واقعی ادمین/اعتبار اجرا شدند؛ فقط موارد کلید-بیرونی مانده‌اند که تقلب نیستند (BLOCKED_EXTERNAL).

## ۹. گیت‌های ضدتکرار (P3)
- **P3-2 گیت کاتالوگ↔تحویل** (`test_catalog_delivery_gate_r10.py`): برای **هر** ردیف `credit_prices`، استحقاقِ تحویل‌داده‌شده با وعدهٔ تیتر مقایسه می‌شود. **یک باگ واقعی گرفت و از رفع آن مطمئن شدیم** (report_audio → audio). این گیت دقیقاً همان دستهٔ باگ را برای همیشه می‌بندد.
- **P3-3 گیت اندپوینت بدون تست** (`test_no_untested_routes_r10.py`): هر `/api/**` که در تست‌ها نیاید → قرمز. Webhookها (نیاز به callback واقعی) و TTS/LLM-probe در `ALLOWED_MISSING` مستند شدند.
- **P3-1/P3-4 (پوشش تعاملی در CI / sweep هفتگی):** با توجه به کندی Playwright، به‌صورت ابزار (`scripts/interaction_sweep.py`) ساخته شد؛ اضافه‌کردن به ci.sh به‌صورت هفتگی توصیه شد (گزینهٔ non-blocking).

## ۱۰. P2-6 — چک‌لیست گوشی واقعی
`docs/qa/PHONE-CHECKLIST-FOR-OWNER.md` — ۲۰ بند برای تست روی iOS Safari / Android Chrome. (فقط مالک می‌تواند؛ موارد ۲/۱۳/۱۵ نیازمند کاوه‌نگار/مرچنت/FCM.)

## ۱۱. آنچه راستی‌آزمایی نکردم (صادقانه)
تولید واقعی (بدون دسترسی) · پرداخت واقعی زرین‌پال (SANDBOX) · SMS واقعی (کاوه‌نگار) · چک‌لیست گوشی فیزیکی. همه نیازمند کلید/دستگاهِ مالک‌اند.

## خلاصه
- ✅ فاز ۰ + P1-1/P1-2 + P2-1/P2-2/P2-5 + **P2-3/P2-4** + **P3-2/P3-3** + P2-6 چک‌لیست
- 🔴 **گیت P3-2 یک باگ واقعی پیدا و رفع کرد** (report_audio → گزارش به‌جای صوت) — اثبات ارزشِ «کاتالوگ↔تحویل».
- **CI: ۷۸۰+ تست سبز (۰ fail) · PR #6 سبز.**
- درس: یافتهٔ پلن درست بود — «کد رندر می‌شود ولی هیچ‌کس کلیک نکرده». اجرای واقعی، باگِ Q1/Q2/P2-5/P3-2 را بیرون کشید.
