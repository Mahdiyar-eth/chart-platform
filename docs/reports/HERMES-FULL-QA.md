# ✅ گزارش اجرای پلن ممیزی انسانی صفر تا صد — HERMES-FULL-QA
**تاریخ:** ۲۰۲۶-۰۸-۲۴ · **پلن:** `docs/plans/PLAN-FULL-QA-v1.md` · **مبنای اجرا:** `main` (شامل R9)

## ۱. صورت‌وضعیت پلن اصلی
`docs/qa/PLAN-STATUS-AUDIT.md` — ۴۰+ بند. DONE ۳۱ · NARROWED ۳ · PARTIAL (نیاز به مالک): F2/G2/G4/G8/Z1.

## ۲. فیکس‌ها (Q1–Q7)
در R9 بسته شدند (rأیt کد در `test_gold_bundle_r9.py` و همکاران).

## ۳. سیاههٔ تعاملی (P2-1)
**۱۳۱۱ آیتم · ۲۸ صفحه** · `docs/qa/INTERACTION-INVENTORY.md`.

## ۴. سوییپ تعاملی (P2-2) — *به‌روز شده در R.10/R1*
**ابزار قابلاتکا شد** (R1): مقایسهٔ **کل DOM + رصد شبکه + URL + توست/دیالوگ** (قبلاً فقط ۳۰۰ کاراکتر اول body).
**نتیجه:**
| حالت | کلیک | صفحات | OK | DEAD | BROKEN |
|---|---|---|---|---|---|
| مهمان | ۵۴ | ۹ | ۵۴ | ۰ | ۰ |
| لاگین | ۳۰ | ۵ | ۳۰ | ۰ | ۰ |
| **مجمول** | **۸۴** | **۱۴** | **۸۴** | **۰** | **۰** |

**مخرج صادقانه (AC-2):** ۸۴ کلیک از ۱۳۱۱ آیتم سیاهه (≈۶.۴٪) — پوشش **حداکثریِ قابلِتست بدون کلید بیرونی**. حالتهای G/U/U+C/U+$ کامل تست شد؛ حالتهای U+P (محصول خریداریشده) و A (ادمین) به کلید/تولید واقعی نیاز دارند.

## ۵. یافته‌های واقعی پیدا و رفع‌شده
| # | منبع | مشکل | فیکس |
|---|---|---|---|
| P2-5 | `/birth-form` | تاریخ نامعتبر بدون خطای فارسی | اعتبارسنجی جلالی/میلادی + روزهای ماه |
| P1-2 | `/plans` | فروش موازی تومانی | جدول واحد اعتباری + `api/purchase` |
| P3-2 | `entitlements.py` | **`report_audio` → `report` به‌جای `audio`** | `_kind_for_action` → `audio` |
| **R4-a** | `/plans` کوپن | دکمهٔ «اعمال» با فیلد خالی بی‌صدا | پیام «کد تخفیف را وارد کن» |
| **R4-b** | `/synastry` | سابمیت خالی → توست فارسی | `novalidate` + اعتبارسنجی JS (پیام) |

## ۶. پنل ادمین (P2-3)
۹ تست (`test_admin_cycles_r10.py`): KPI · زیاد-اعتبار · تغییر قیمت · اعطای دستی · فلگ · سلامت · پرامپت · صفحهٔ محتوا · ۴۰۳.

## ۷. سفرهای کاربر (P2-4)
۵ تست (`test_user_journeys_r10.py`): خرید پک · GOLD→چت+گذر · هزینه گزارش · ناکافی-بدون-بدهی · گذر با استحقاق.

## ۸. گیت ضدتکرار (P3)
- **P3-2 کاتالوگ↔تحویل** (`test_catalog_delivery_gate_r10.py`) — باگ `report_audio` را گرفت.
- **P3-3 مسیر بدون تست** (`test_no_untested_routes_r10.py`) — `credit-report` را لو. داد.
- **P3-1/P3-4 پوشش تعاملی در CI:** ابزار آماده + تست قابلاتکا (`test_sweep_tool_reliable_r10.py`).

## ۹. P2-6 چک‌لیست گوشی
`docs/qa/PHONE-CHECKLIST-FOR-OWNER.md` — ۲۰ بند.

## ۱۰. NARROWED — بندهایی که کوچک‌تر اجرا شدند (AC-5)
| بند | NARROWED به | چرا |
|---|---|---|
| **P2-2** | ۸۴ کلیک (۶.۴٪)، نه ۱۰۰٪ | بدون کلید بیرونی، تستِ همهٔ ۱۳۱۱ آیتم در ۶ حالت عملاً شدنی نیست (پرداخت/OTP/TTS/ادمین نیاز به کلید دارد). عدد عمداً صریح شد. |
| **P2-3** | ۹ چرخه، نه ۱۳ | ۳ چرخهٔ باقی (CMS کامل، رسانه، کوپن با مصرف) نیاز به داده/کلید دارد. |
| **P2-4** | ۵ سفر، نه ۹ | ۴ سفرٔ باقی (OTP/زرین‌پال/FCM) کلید بیرونی می‌خواهد. |

## ۱۱. آنچه راستی‌آزمایی نکردم (صادقانه)
تولید واقعی · پرداخت واقعی زرین‌پال · SMS واقعی · گوشی فیزیکی — همه نیازمند کلید/دستگاهِ مالک.

**درس:** یافتهٔ پلن درست بود — «کد رندر می‌شود ولی هیچ‌کس کلیک نکرده». R1+R3+R4+R4a/R4b از اجرای واقعی بیرون آمدند. دو گیت P3-2/P3-3 باارزش‌ترین خروجی است.
