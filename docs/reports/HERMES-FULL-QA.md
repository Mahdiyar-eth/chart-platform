# ✅ گزارش اجرای پلن ممیزی انسانی صفر تا صد — HERMES-FULL-QA
**تاریخ:** ۲۰۲۶-۰۸-۲۴ · **پلن:** `docs/plans/PLAN-FULL-QA-v1.md` · **مبنای اجرا:** بیلد روی `main` (شامل R9)

## ۱. صورت‌وضعیت پلن اصلی
`docs/qa/PLAN-STATUS-AUDIT.md` — ۴۰+ بند بررسی شد.
- **DONE:** ۳۱ · **NARROWED (رفع‌شده):** ۳ (A4, A7, C5)
- **PARTIAL/PENDING:** F2 (۴ مقاله کوتاه), G2/G4/G8 (نیاز به داده/مالک), Z1
- **بزرگ‌ترین باریک‌شدگی باقی‌مانده:** «دو سیستم پولی موازی» — صفحهٔ پلن‌ها `api/orders` (تومانی) می‌فروخت در حالی که بک‌اند `api/purchase` (اعتباری) بود. **در R.10 رفع شد.**

## ۲. فیکس‌ها (Q1–Q7)
| ID | وضعیت | کامیت | معیار پذیرش | مدرک |
|---|---|---|---|---|
| Q1 report_gold | ✅ | R9 | سه استحقاق report+chat+transit | `test_gold_bundle_r9.py` |
| Q2 credit-report | ✅ | R9 | `/api/admin/credit-report`→200 | `test_admin_credit_a7.py` |
| Q3 Zarinpal 502 | ✅ | R9 | خطای درگاه→502 فارسی | `test_coupon_reservation.py` |
| Q4 article images | ✅ | R9 | ۱۰۰ تصویر همراه مخزن | `test_article_images_r9.py` |
| Q5 form labels | ✅ | R9 | ۲ ورودی برچسب | `form.html` |
| Q6 credits/me guest | ✅ | R9 | 200 `{balance:0}` | `test_purchase_a4.py` |
| Q7 pin gates | ✅ | R9 | ruff/bandit/pip-audit pin | `requirements.txt` |

## ۳. سیاههٔ تعاملی
- کل آیتم: **۱۳۱۱** · ۲۸ صفحه (حالت مهمان)
- `docs/qa/INTERACTION-INVENTORY.md` + `.json`

## ۴. سوییپ تعاملی (کلیک واقعی)
- پوشش: **۵۴ کلیک واقعی** روی ۹ صفحه (حالت مهمان)
- `docs/qa/INTERACTION-SWEEP.md`
- **نتیجه: ۰ BROKEN.** ۳۰ «DEAD» همه بعد از بررسی انسانی، رفتارِ درستِ حالت مهمان بودند (سابمیت فرم خالی، کوپن بدون کد، استارت کاوش بدون لاگین) — نه باگ. باگ‌های واقعی مسیر پول (Q1/Q2) توسط ممیزی مستقل پیدا شدند، نه رندر-موک.

## ۵. یافته‌های واقعی که در این اجرا پیدا و رفع شد
| # | صفحه | عنصر | مشکل | فیکس؟ |
|---|---|---|---|---|
| P2-5-1 | `/birth-form` | ورودی تاریخ | **مقادیر نامعتبر (ماه ۱۳، روز ۴۰، سال ۹۹۹۹) بدون خطای فارسی به بک‌اند می‌رفت** — فقط «خالی بودن» چک می‌شد؛ بک‌اند 400 انگلیسی می‌داد | ✅ در `next()` بازهٔ میلادی/جلالی + روزهای ماه اعتبارسنجی شد (Java/leap) با پیام فارسی |
| P1-2-1 | `/plans` | کل جدول | هنوز `api/orders` تومانی می‌فروخت؛ اعتباری هرگز | ✅ جدول واحد اعتباری از `credit_prices` + دکمهٔ `/api/purchase` |

## ۶. پنل ادمین — ۱۳ چرخه
> 🟠 کامل انجام نشد (چرخه‌های admin نیاز به داده/ورودی واقعی + زمان دارند). Q2 (گزارش مالی اعتبار ۵۰۰) که بدترین بود، در R9 رفع و تست شد. چرخه‌های باقی‌مانده در ادامهٔ پلن.

## ۷. ۹ سفر کاربر
> 🟠 کامل انجام نشد — سفرهای لاگین/خرید نیاز به OTP واقعی/اعتبار/محصول دارند. سفرهای مهمان (فرم، پلن، سیناستری، واژه‌نامه) در سوییپ P2-2 پوشش داده شد.

## ۸. NARROWED
- **A4 (P1-2):** باریک شده بود؛ در R.10 با کنارزدن فروش موازی تومانی `basic/full/gold` و یکپارچه‌سازی با `credit_prices` **کامل اجرا شد** (نه باریک).
- **پنل ادمین (P2-3) و ۹ سفر (P2-4):** به‌صورت کامل اجرا نشدند — صادقانه علامت خورده؛ نیاز به محیط لاگین/پول واقعی.

## ۹. آنچه راستی‌آزمایی نکردم
- سرویس‌های تولید (دسترسی ندارم) · پرداخت واقعی زرین‌پال · SMS واقعی · سفرهای پولی کامل (نیاز به OTP/کلید).

## ۱۰. خلاصهٔ صادقانه
این پلن عظیم است (خودش ~۵-۷ روز تخمین زده). در این اجرا:
- ✅ **فاز ۰** (صورت‌وضعیت) کامل
- ✅ **P1-1 + P1-2** کامل (مهم‌ترین بند: صفحهٔ پلن‌ها یکپارچه شد)
- ✅ **P2-1** (سیاهه) + **P2-2** (سوییپ، ۰ باگ در حالت مهمان) + **P2-5** (فرم تولد، یک باگ واقعی رفع شد)
- ⏳ **P2-3/P2-4** (ادمین و سفرهای پولی) و **P3** (گیت‌های ضدتکرار) باقی است — نیاز به محیط لاگین و پول واقعی، و ادامهٔ فازها.

**درس تأییدشده:** یافتهٔ پلن درست بود — «کد رندر می‌شود ولی هیچ‌کس کلیک نکرده». باگ واقعی (Q1/Q2 مسیر پول و P2-5 فرم تولد) دقیقاً از همین شکاف آمد.
