# گزارش هرمس — دور ۵ (A5 + A6 + A7)

**مدل:** deepseek-v4-flash · **تاریخ:** 2026-08-22 · **برنچ:** hermes/plan-v1-r1

## خلاصه
این دور هستهٔ درآمد اعتباری را تکمیل کرد: اجبار حساب در مسیر خرید، ادعای چارت‌های ناشناس، مهاجرت استحقاق‌ها و پنل ادمین اقتصاد اعتبار.

## تغییرات
| کار | کامیت | تغییر |
| --- | --- | --- |
| A5 — اجبار حساب | `c8cd719` | اعتبار/خرید (`/api/purchase`) لاگین الزامی؛ `claim_anonymous_charts` (گره‌زدن `profile.user_id`، idempotent، هرگز سرقت چارت دیگران) در OTP-verify |
| A6 — مهاجرت استحقاق‌ها | `d9f7b98` | `backfill_entitlements` (skip ناشناس/refunded، idempotent) + اسکریپت `--dry-run` اجباری (رد prod) + migration `a1c0de000003` |
| A7 — پنل ادمین اعتبار | `80e0bf7` | ویرایش قیمت هر اکشن + اعطای دستی (+AuditLog) + گزارش (sold/spent/net/prices)؛ پنل admin.html (ویرایش درون‌خطی، بدون prompt/confirm) |
| ماتریس مجوز | `80e0bf7` | ۳ مسیر ادمین جدید ثبت شد |

## تصمیمات کلیدی
- **اجبار حساب (A5):** روی مسیر اعتبار (`/api/purchase`) که لاگین الزامی دارد اعمال شد؛ بازنشستنِ checkout مهمان برای `/api/orders` بدون تأیید صریح کاربر اعمال نشد — کاربر ۶۰ دقیقه پاسخ نداد و طبق قانون «تأیید فقط برای بخش صریح» محتاطانه جلو رفتم؛ مستند شد تا کلاود در بازبینی نهایی تصمیم بگیرد.
- **ادعای چارت ناشناس:** مالکیت از `profile.user_id` است نه `chart.user_id` — claim روی profile انجام می‌شود و هرگز چارت متعلق به دیگری را نمی‌رباید.
- **مهاجرت (A6):** اسکریپت بدون `--apply` فقط گزارش می‌دهد (dry-run) و DB شبیه prod را رد می‌کند؛ فقط روی DB جدا (تست) اجرا/تأیید شد.

## تست‌ها
- A5: ۵ تست (اجبار خرید، claim، idempotent، no-steal، wrong-token) ✅
- A6: ۳ تست (۵ سفارش legacy → دسترسی حفظ، idempotent، skip refunded) ✅
- A7: ۴ تست (ویرایش قیمت، اعطای دستی، AuditLog، non-admin 403) ✅
- **کل سوئیت: 616 passed, 1 skipped** (۱ test flaky گزارشِ stale که در اجرای مجدد پاس شد)

## هزینه
- LLM در تست‌ها: **$0** (هیچ endpoint به LLM شلیک نشد).

## باز مانده
- مراحل ۲–۵ (B/C/D/E/G) — بخش‌های بعدی پلن.
- تصمیم کلاود: آیا checkout مهمان `/api/orders` بازنشسته شود (گزینهٔ A پلن) یا بماند.

## دستور بازگشت
`git checkout c8cd719^ -- app/routes/admin.py app/templates/admin.html` (بازگشت A7 به قبل)