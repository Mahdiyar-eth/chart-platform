# 🎯 ZAYCHE — PLAN FINAL (بستن نهایی — ۲۰۲۶-۰۸-۱۷)

> همهٔ ۷ فاز قبلی بستهاند. این پلن فقط باقیماندههای راستیآزماییشده را میبندد — بعد از آن «تمام».

## P1 — ماتریس routing واحد (کد + پنل + سند) — ~۱ ساعت
- منوی مدل پنل ادمین: افزودن `gemini-3.6-flash-high` + گزینهٔ provider `auto→omni` (فعلاً فقط env) به سکشن «پروایدر و مدل هوش مصنوعی»
- افزودن `section_model_default` به SECRET_CATALOG تا سکشنها هم از پنل عوض شوند
- پیشفرض preview → `deepseek-v4-flash` (ماتریس تاییدشده: Chat=gemini-high · Preview=flash · Report=gemini-high · سکشنها=gemini-high)
- `docs/ROUTING.md` منبع واحد + فیکس جداول FINAL-GO/MODEL-TEST-1
- بعد از تغییر: تست اتصال زنده از پنل + ثبت شواهد

## P2 — M3 با پنجرهٔ تمیز — خودکار (کرون ثبتشده ۱۹:۳۰ فردا)
- خروج طبیعی ردیفهای قدیمی از پنجرهٔ ۲۴h → گزارش خودکار p95
- شاهد میانی الان: ۱۰ اجرای آخر = ۹-۱۷ ثانیه (زیر گیت ۴۰)

## P3 — کانون سکشنها — ~۳۰ دقیقه
- **basic=5 · full=13 · gold=14** (کد: PLAN_SECTIONS در prompt_builder + DOMAINS در rules)
- اصلاح «۱۵/۱۵» در PHASE3-4-SPEED-AB و FINAL-GO (اشتباه من: شمارش calls با retry بود)
- افزودن تست رگرسیون روی تعداد سکشنها

## P4 — باگهای REAL ممیزی opus (راستیآزماییشده) — ~۴ ساعت
1. **F-29 (پولی)**: pay_order_with_balance باید `yearly` را اکتیو کند + به `credit3/credit6` اعتبار بدهد 2. **F-11**: انتقال سید کوپن داخل `with Session` (کوپن LANCH20 واقعاً سید شود)
3. **F-37**: رفع دکمهٔ تأیید لغو اشتراک (dataset روی عنصر درست)
4. **F-26**: اعتبارسنجی خانهها با خانههای واقعی پلاسیدوس (کاهش ریترایهای بیهوده = کیفیت + هزینه)
5. **F-20**: جمعآوری متریکها per-task و merge (آمار دقیق هزینه)
6. **F-08**: escape متن هفتگی (فقط <br> مجاز) + **F-35**: سندِ scope توکن share
7. تست ۵۳۹+ باگهای جدید سبز + deploy + شواهد E2E

## P5 — شخصیسازی درجهٔ یک — ~۲ ساعت
- هدف: **Personalization ≥ 8 روی روبریک رسمی برای gemini (الان 6.33) و ≥ 8.5 برای pro/sonnet (الان 7.5)**
- پرامپت v3: تقویت هویت زندگینامهای (سن/شهر/وضعیت از پروفایل)، ۳ واقعیت اجباری شخصی در هر پاسخ،
  ممنوعیت جملهٔ عمومی سنگینتر، مثالهای درست/غلط شخصیسازی در system prompt
- ارزیابی قبل/بعد روی ۱۰ چارت × ۳ مدل (harness فاز ۲)
- در صورت موفقیت deploy؛ بعداً «حالت عمیق» (Pro+thinking) بهعنوان محصول خاص (backlog)

## P6 — FINAL-GO v2 + بستن — ~۳۰ دقیقه
- ماتریس نهایی + کانون 5/13/14 + نتایج ممیزی opus و راستیآزمایی آن + M3
- عنوان: **Code Complete ✅ · AI Ready ✅ · Launch = اقدامات سمت کاربر 🟡**
- commit + push + تحویل بستهٔ نهایی به تلگرام

## تعریف «تمام»
بعد از P1-P6: هیچ آیتم فنی باز نمیماند.
باقیماندهٔ Launch فقط سمت کاربر است: مرچنت زرینپال، کاوهنگار، دامنهٔ zayche.io، push، Search Console، تست گوشی.

## بودجه
opus ممیزی: ~$3.0 (تأییدشده). P5 ارزیابی: ~$0.5. بقیه بدون هزینهٔ API.