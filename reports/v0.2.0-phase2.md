# گزارش فاز ۲ — محصول رایگان (v0.2.0)

**تاریخ:** ۲۲ مرداد ۱۴۰۵ (۲۰۲۶-۰۸-۱۲) | **کامیت:** 8b71993 | **وضعیت:** ✅ کامل

## خلاصه
محصول رایگان کامل: لندینگ + فرم ۵ مرحلهای + محاسبه + صفحهی چارت با Big Three.

## تغییرات
- `app/main.py` — FastAPI: `/` (لندینگ)، `/birth-form` (فرم)، `/chart/{id}` (چارت)، `/api/charts`، `/api/cities`
- `app/models.py` — SQLModel: users → birth_profiles → charts (gender اختیاری، طبق بازبینی کلاود)
- `app/astrology/big_three.py` — Big Three با کلیدهای تفسیر ساختاریافته
- `app/astrology/cities_ir.py` — search_cities (نرمالسازی ي عربی، شهرهای محبوب)
- تمپلیتها: base (Liquid Glass RTL)، index، form (۵ گام Alpine)، chart
- فرم: شمسی/میلادی، ساعت دقیق/ناشناخته، جستجوی شهر، ۱۳ حوزه، سؤال اختیاری

## تستها
- pytest: ۲۱ پاس (Golden Suite) ✅
- Playwright E2E موبایل (390px): فرم کامل → چارت → redirect ✅ بدون خطای JS
- Vision QA (Gemini): لندینگ «Highly Polished»، صفحهی چارت PASS (RTL، بدون overflow)

## یافتهها
- Pyright روی SQLModel `__tablename__` خطای تایپ میدهد — الگوی استاندارد، runtime OK
- GRANT روی schema public برای کاربر جدید لازم است
- `Alpine.$data(form)` در Playwright بهصورت dict برمیگردد (دسترسی با `d["key"]`)

## هزینه
۰ دلار (Gemini فری + محاسبه محلی)

## کامیت بعدی
فاز ۳: موتور گزارش (Section schema، Prompt versioning، QA خودکار، PDF، صف ARQ)

## Rollback
`git checkout 0666598`
